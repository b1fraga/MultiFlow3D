name: Lint with Fortitude

on:
  workflow_dispatch:
  pull_request:
    branches: [fusion-forests-project]
  push:
    branches: [fusion-forests-project]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  test:
    name: Fortitude-Lint
    runs-on: [ubuntu-latest]
    strategy:
      fail-fast: false
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13' 
      - run: |
          python -m pip install fortitude-lint
          fortitude check --select MOD021 --select S071 --output-format github
